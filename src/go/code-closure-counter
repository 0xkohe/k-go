package main

func makeCounter() func() int {
    count := 0;
    return func() int {
        count = count + 1;
        return count;
    };
}

func main() {
    counter := makeCounter();
    print(counter());
    print(counter());
    print(counter());
}
